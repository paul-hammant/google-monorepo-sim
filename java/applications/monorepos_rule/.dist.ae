import bldr
import java

aeb(cap) {
    bldr.build() {
        dep("java/applications/monorepos_rule/.build.ae")
        java.shade("applications.monorepos_rule.MonoreposRule", "monorepos-rule.jar")
    }
}
