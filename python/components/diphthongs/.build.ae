import bldr
import bldr (prereq)
import python
import python (pip)

aeb(cap) {
    bldr.build() {
        prereq("python:3.12")
        python.install() {
            pip("pytest")
        }
        python.lint()
        python.typecheck()
    }
}
