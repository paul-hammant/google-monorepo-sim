declare module 'libs/javascript/ffi-napi/lib/ffi' {
    // This is a minimal declaration to satisfy TypeScript.
    // It declares the module and exports a default 'any' type,
    // which is common for untyped JavaScript modules.
    const ffi: any;
    export = ffi;
}
