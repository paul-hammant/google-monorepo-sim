import bldr
import java

aeb(cap) {
    bldr.build() {
        dep("java/components/glides/.build.ae")
        dep("libs/java/junit/.junit.jar.ae")
        dep("libs/java/hamcrest/.hamcrest.jar.ae")
        java.javac_test()
        java.junit()
    }
}
