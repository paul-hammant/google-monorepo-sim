import bldr
import java

aeb(cap) {
    bldr.build() {
        dep("java/components/sibilants/.build.ae")
        dep("libs/java/junit-fetched/.junit-fetched.jar.ae")
        java.javac_test()
        java.junit()
    }
}
