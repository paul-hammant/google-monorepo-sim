import build
import java

main() {
    b = build.start()
    build.dep(b, "java/components/sibilants/.build.ae")
    build.dep(b, "libs/java/junit-fetched/.junit-fetched.jar.ae")
    java.javac_test(b)
    java.junit(b)
}
