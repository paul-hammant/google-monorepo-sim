import build
import ts

main() {
    b = build.start()
    build.dep(b, "typescript/components/explanation/.build.ae")
    build.dep(b, "go/components/nasal/.build.ae")
    build.dep(b, "libs/javascript/ffi-napi/.ffi-napi.npm.ae")
    ts.tsc_files(b)
}
