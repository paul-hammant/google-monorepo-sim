import bldr
import bldr (prereq)
import go
import go (build_mode, output_file)

aeb(cap) {
    bldr.build() {
        // States the toolchain this job needs; requester picks aeb-tc:go.
        prereq("go:1.22")
        go.go_build() {
            build_mode("c-shared")
            output_file("libgonasal.so")
        }
    }
}
