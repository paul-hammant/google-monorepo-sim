import build
import kotlin

main() {
    b = build.start()
    build.dep(b, "kotlin/components/sonorants")
    kotlin.kotlinc_test(b)
    kotlin.kotlin_test(b, "components.sonorants.SonorantsTestsKt")
}
