import bldr
import kotlin

aeb(cap) {
    bldr.build() {
        dep("kotlin/components/sonorants/.build.ae")
        kotlin.kotlinc_test()
        kotlin.kotlin_test("components.sonorants.SonorantsTestsKt")
    }
}
