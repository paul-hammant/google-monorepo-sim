import bldr
import ts

aeb(cap) {
    bldr.build() {
        dep("typescript/components/explanation/.build.ae")
        dep("go/components/nasal/.build.ae")
        dep("libs/javascript/ffi-napi/.ffi-napi.npm.ae")
        ts.tsc_files()
    }
}
