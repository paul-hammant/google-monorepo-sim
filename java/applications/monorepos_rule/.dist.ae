import build
import java

main() {
    b = build.start()
    build.dep(b, "java/applications/monorepos_rule/.build.ae")
    java.shade(b, "applications.monorepos_rule.MonoreposRule", "monorepos-rule.jar")
}
